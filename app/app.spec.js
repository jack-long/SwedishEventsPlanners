// describe('SEPController', function() {

//   beforeEach(module('sepApp'));

//   it('should create a `phones` model with 3 phones', inject(function($controller) {
//     var scope = {};
//     var ctrl = $controller('SEPController', {$scope: scope, DB: db});

//     expect(db.event_requets.length).toBe(3);
//   }));

// });


// describe('login', function() {

//   // Load the module that contains the `phoneList` component before each test
//   beforeEach(module('sepApp'));

//   // Test the controller
//   describe('loginController', function() {

//     it('should create a `password` with value `password`', inject(function($componentController) {
//       var ctrl = $componentController('login');

//       expect(ctrl.password).toBe('password');
//     }));

//   });

// });